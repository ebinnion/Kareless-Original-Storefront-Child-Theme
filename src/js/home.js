// ( function( $, ofi ) {
// 	ofi( $( 'img.kareless-image-fill' ) );
// } )( jQuery, objectFitImages );

jQuery( function () {
	objectFitImages( jQuery(
		'.kareless-image-fill-container img'
	) );
} );